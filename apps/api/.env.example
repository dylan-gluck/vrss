# =============================================================================
# VRSS API - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string for Prisma ORM
DATABASE_URL="postgresql://vrss_user:vrss_dev_password@localhost:5432/vrss?schema=public"

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
# JWT secret for token signing (not used with better-auth, kept for compatibility)
JWT_SECRET=dev_jwt_secret_change_in_production
JWT_EXPIRES_IN=7d

# Better-auth Configuration
# Secret for signing tokens (minimum 32 characters)
BETTER_AUTH_SECRET=dev_better_auth_secret_minimum_32_chars_required_for_security_12345
APP_URL=http://localhost:3000
WEB_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Email Configuration
# -----------------------------------------------------------------------------
# Email provider: console | smtp | sendgrid | auto
# - console: Logs emails to console (development)
# - smtp: Sends emails via SMTP (Nodemailer)
# - sendgrid: Sends emails via SendGrid API
# - auto: Auto-detect based on NODE_ENV and available credentials (default)
EMAIL_PROVIDER=auto

# SMTP Configuration (if EMAIL_PROVIDER=smtp)
# Example for Gmail:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_SECURE=false
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASS=your-app-password
# Example for AWS SES:
#   SMTP_HOST=email-smtp.us-east-1.amazonaws.com
#   SMTP_PORT=587
#   SMTP_SECURE=false
#   SMTP_USER=your-aws-access-key-id
#   SMTP_PASS=your-aws-secret-access-key
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="VRSS <noreply@vrss.dev>"

# SendGrid Configuration (if EMAIL_PROVIDER=sendgrid)
SENDGRID_API_KEY=
SENDGRID_FROM=noreply@vrss.dev

# -----------------------------------------------------------------------------
# Storage Configuration
# -----------------------------------------------------------------------------
STORAGE_TYPE=local

# -----------------------------------------------------------------------------
# Development Tools
# -----------------------------------------------------------------------------
# Set to true to enable verbose logging
DEBUG=false
